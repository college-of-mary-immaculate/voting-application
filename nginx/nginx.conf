events {}

http {
    upstream frontend_servers {
        server frontend1:80;
        server frontend2:80;
        server frontend3:80;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://frontend_servers;
            proxy_set_header Accept-Encoding "";
        }
    }
}