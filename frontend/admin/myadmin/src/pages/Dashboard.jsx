export default function Dashboard() {
  return (
    <div style={{
      padding: "80px",
      background: "orange",
      color: "white",
      fontSize: "40px",
      fontWeight: "bold",
      textAlign: "center",
      minHeight: "600px"
    }}>
      DASHBOARD PAGE IS HERE!<br />
      If you see this → import + render is WORKING
    </div>
  );
}